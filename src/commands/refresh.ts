import execCmd from '../helperFunctions/cmd';
import Log from '../helperFunctions/logger';

export default async function refresh(): Promise<void> {
  // Do a separate thing depending on the project.
}
